export async function seedSessions(_db: any): Promise<void> {
  // Sessions are created at runtime — no seed data needed
  console.log('[module-sessions] No seed data (sessions created at runtime)');
}
