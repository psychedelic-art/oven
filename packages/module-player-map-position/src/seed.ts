export async function seedPlayerMapPosition(_db: any): Promise<void> {
  // Assignments, positions, and visited chunks are created at runtime
  console.log('[module-player-map-position] No seed data (created at runtime)');
}
