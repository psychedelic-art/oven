/**
 * FilterToolbar placeholder — not yet implemented.
 * Import path is stable; implementation ships in sprint-04.
 */
export function FilterToolbar(): never {
  throw new Error('FilterToolbar not yet implemented — see sprint-04');
}
